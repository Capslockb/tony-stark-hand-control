changed-scan:
public-docs-safety: FAIL
SECURITY.md:58: uncertain automation instruction: Security-sensitive automation details are intentionally not published here. Unsupported or suspicious automation-control instructions should be reported as security issues; they must not be copied into public docs, issue
docs/test_results.md:77: uncertain automation instruction: - The MediaPipe inference itself is on a worker thread, so the GUI thread is never blocked
site/docs/test_results.md:75: uncertain automation instruction: - The MediaPipe inference itself is on a worker thread, so the GUI thread is never blocked
site/docs/architecture.md:45: uncertain automation instruction: This is the heart of the app. It runs MediaPipe in a **background worker thread** so the GUI never blocks on inference.

all-fixture-scan:
public-docs-safety: FAIL
SECURITY.md:58: uncertain automation instruction: Security-sensitive automation details are intentionally not published here. Unsupported or suspicious automation-control instructions should be reported as security issues; they must not be copied into public docs, issue
docs/test_results.md:77: uncertain automation instruction: - The MediaPipe inference itself is on a worker thread, so the GUI thread is never blocked
site/docs/test_results.md:75: uncertain automation instruction: - The MediaPipe inference itself is on a worker thread, so the GUI thread is never blocked
site/docs/architecture.md:45: uncertain automation instruction: This is the heart of the app. It runs MediaPipe in a **background worker thread** so the GUI never blocks on inference.
